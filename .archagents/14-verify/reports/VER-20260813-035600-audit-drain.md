---
id: VER-20260813-035600-audit-drain
run: RUN-20260813-035500-audit-drain
design: DES-0012
ticket: TCK-0012
covers: [TCK-0001, TCK-0002, TCK-0003, TCK-0004, TCK-0005, TCK-0006, TCK-0007, TCK-0008, TCK-0009, TCK-0010, TCK-0011, TCK-0012]
verified_at: 2026-08-13T03:56:00Z
verified_by: grok-ship-verify-inline
context: shared
verdict: approved-with-notes
---

# Verify Report — VER-20260813-035600-audit-drain

**Veredito:** `approved-with-notes` — critérios mecânicos dos tickets de código passam e o scan de secrets está limpo; o escopo é uma onda inteira (12 TCKs + overlays) sem DES por ticket, e o AS-IS `_meta.json` ficou defasado.

**Independência:** fallback inline declarado (P10). Checks re-executados (não lidos do REPORT do execute). `verified_by` ≠ `operator` do RUN.

## Dim 0 — Criteria Gate

Tickets trusted-local desta sessão. `criteria-check.py` do harness não foi o único consumidor: aceite TCK-0012 re-checado à mão:

- `ensureHydratedStorage` cobre web + IO — `packages/micro_apps/payments/lib/src/bootstrap/hydrated_storage_bootstrap.dart:L11-L21`
- idempotente — teste `ensureHydratedStorage is idempotent` pass
- `PaymentsCubit` sem storage lança `StorageNotFound` — teste pass
- `payments_micro_app` sem `if (!kIsWeb)` no build — `rg` 0 matches
- `main()` chama ensure antes dos micro-apps — `super_app/lib/main.dart:L19-L22`

Waiver: TCK-0001–0011 já marcados `done` no drain sem VER prévio; este VER cobre a onda (`covers:`).

## Correctness (Dim 1 + 6)

- Auth session: `establishSession` + splash/repo — testes auth session + `auth_redirect_test` pass.
- Pagamentos web: Hive `hydrated_box` no boot Chrome; `payments_loaded count:3`.
- Overflow: chart viewport 260 — `transaction_chart.dart` + 6 widget tests pass.
- Pix close: getter recria se `isClosed` — `pix_micro_app.dart` getter; `add` guardado nas pages.

## Safety (Dim 7)

- `gitleaks-scan.sh` rc=0 no working tree.
- Senha de demo continua só como credencial de sample (README / mock) — não é secret de cloud.
- `android/local.properties` **não** entra no commit (path local de SDK).
- Interceptor redige headers — `logging_interceptor.dart` (TCK-0001).
- Sem `eval`/SQL concat/exec no diff de Dart de negócio.

## Scope (Dim 5)

⚠️ Onda agregada: CI, pubspecs, iOS/macOS Flutter auto-migrate, design system, 12 tickets. Fora de qualquer DES único (só DES-0012 existe). Aceito como drain de audit + overlays documentados no RUN. `packages/*/build/` e snapshots excluídos do ship.

## Observações / follow-ups

- [FIX-REQUIRED] não — notas advisory.
- Follow-up: iOS sim / ML Kit; `PaymentsCubit` morto em `presentation/bloc/`; `hydrated_bloc` órfão em pubspecs; hashes de `_meta.json` AS-IS desatualizados (`/ops-config docs`).
- iOS 15 no Podfile é exigência do Xcode 26 local, não do CI pin 3.29.2.
