# Events

Eventos de produto instrumentados para observabilidade.

**Schema:** `{name, trigger, payload, privacy}`

Cada evento deve declarar claramente o gatilho, a estrutura do payload e as considerações de privacidade (PII, LGPD).
